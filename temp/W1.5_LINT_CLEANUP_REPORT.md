# W1.5 — API lint temizleme raporu

> **Tarih:** 4 Ağustos 2026
> **Dal:** `main` (kullanıcı brief'inde `feature/pilot-prep` yazıyordu; gerçek dal `main`)
> **Kapsam:** `apps/api` — W1.2 lab/imaging modülleri + `ai/` yardımcı dosyaları
> **Sonuç:** API lint `--max-warnings=0` kapısı 0 hata ile geçiyor.

---

## 1. Brief ile gerçek durum arasındaki farklar

Brief'te yer alan sayılar ve varsayımlar, çalışma ağacının gerçek durumuyla
**önemli ölçüde** farklıydı. Doğru sayılar aşağıdadır.

| Brief                              | Gerçek (ölçülen)                                         |
| ---------------------------------- | -------------------------------------------------------- |
| 1.520 ESLint hatası                | 224 problem (207 error, 17 warning)                      |
| Security eklentisinin 95 uyarısı   | 17 warning'ın 4'ü security; kalan 13'ü JSDoc/custom tag  |
| Branch: `feature/pilot-prep`       | `main`                                                   |
| W1.2 modülleri commitlenmiş         | Hayır — dirty working tree; 88/88 unit test geçiyor     |
| `--max-warnings=95` eşiği          | `--max-warnings=0` (her uyarı kapıyı kırıyor)           |

Brief'in 1.520 rakamı muhtemelen EXECUTION_ROADMAP'teki eski bir ölçüm
(1.228 → 1.158 → 1.151 → 141 → …) ile karıştırıldı. Mevcut çalışma ağacında
API zaten büyük ölçüde temizdi; kalan gerçek iş **224 → 20 → 0** akışıyla
ilerledi.

---

## 2. Yapılan iş (akış)

### 2.1. Otomatik düzeltme (eslint --fix)

İlk çalıştırmada 224 problem vardı. `--fix` ile **204 tanesi otomatik
çözüldü**. Kalan 20 (3 error + 17 warning) elle müdahale gerektirdi:

- 17 × `jsdoc/check-tag-names` — W1.2 iş item referansları (`@w1.2a/b/c/d`,
  `@w1.4`, `@note`) `jsdoc` plugin'in default closure tag listesinde yok.
- 1 × `@typescript-eslint/no-unused-vars` `ImagingContrastUse` (imaging-orders)
- 1 × `@typescript-eslint/no-unused-vars` `Prisma` (lab-results)
- 1 × `@typescript-eslint/no-unsafe-member-access` (ai-rag-index test)
- 1 × `security/detect-non-literal-fs-filename` (ai-chunk-loader)
- 2 × `security/detect-object-injection` (imaging-orders approve_report)

### 2.2. Yapısal düzeltme kararları

| Karar                                                                | Gerekçe |
| -------------------------------------------------------------------- | ------- |
| `jsdoc/check-tag-names` → `definedTags: [w1.2a/b/c/d, w1.4, note]`   | Custom JSDoc tag'leri proje-internal takip isimleri. Her dosyada suppression yerine tek noktada whitelist. |
| `@w1.2a` (lab-tests) import sırası düzeltildi                       | W1.2a yeni `@prisma/client` import'u parent/external sırasını bozmuştu. |
| `imaging-orders.approveReport` audit objesi                         | `updated[lastIdx]!.x` object-injection uyarısı; map callback içinde revision/portalVisible typed snapshot'a alındı, son eleman `Array.prototype.at(-1)` literal index ile okundu. |
| `ai-chunk-loader.stat(this.filePath)`                               | `this.filePath` constructor'da set edilen kontrollü yol; gerekçeli `eslint-disable-next-line`. |
| `ai-rag-index.spec.ts` cache test                                   | Mevcut suppression yanlış kuralı (`no-explicit-any`) kapsıyordu; doğru kural (`no-unsafe-member-access`) eklendi. |

`any` kullanımı: Yeni `any` eklenmedi. Mevcut `any` daraltma fırsatları bu
çalışmanın dışında; mevcut test mock'ları zaten global `any` kuralından
`*.spec.ts` istisnası ile muaf.

---

## 3. Modül bazında düzeltilen dosyalar

### 3.1. `apps/api/src/modules/lab-tests/` (W1.2a)

- `lab-tests.module.ts` — JSDoc @w1.2a tag'i
- `lab-tests.repository.ts` — `@w1.2a` + `@w1.4` JSDoc; import sırası düzeltildi (`@prisma/client` önce, sonra `@vetniva/contracts`, sonra internal `LabTestRecord`)
- `lab-tests.service.ts` — JSDoc @w1.2a tag'i
- `lab-tests.service.spec.ts` — `@note` ve `@w1.2a` JSDoc tag'leri

### 3.2. `apps/api/src/modules/lab-orders/` (W1.2b)

- `lab-orders.module.ts`, `lab-orders.repository.ts`, `lab-orders.service.ts` — `@w1.2b` JSDoc tag'i

### 3.3. `apps/api/src/modules/lab-results/` (W1.2c)

- `lab-results.module.ts`, `lab-results.repository.ts`, `lab-results.service.ts` — `@w1.2c` JSDoc tag'i
- `lab-results.repository.ts` — kullanılmayan `Prisma` type import'u kaldırıldı (**error**)

### 3.4. `apps/api/src/modules/imaging-orders/` (W1.2d)

- `imaging-orders.module.ts`, `imaging-orders.repository.ts`, `imaging-orders.service.ts` — `@w1.2d` JSDoc tag'i
- `imaging-orders.service.ts:39` — kullanılmayan `ImagingContrastUse` import'u kaldırıldı (**error**)
- `imaging-orders.service.ts:approveReport()` — `updated[lastIdx]!.revision/portalVisible` erişimleri `security/detect-object-injection` üretiyordu; map callback içinde yerel typed değişkenlere alındı, `Array.prototype.at(-1)` literal index ile son revizyona erişildi (**2 warning**)

### 3.5. `apps/api/src/common/ai/` ve `apps/api/src/modules/ai/`

- `ai-chunk-loader.service.ts:86` — `security/detect-non-literal-fs-filename` warning'i; gerekçeli suppression eklendi
- `ai-rag-index.service.spec.ts:273` — `@typescript-eslint/no-unsafe-member-access` error'ı; doğru rule ile suppression güncellendi

### 3.6. `apps/api/eslint.config.mjs`

- `jsdoc/check-tag-names` rule'una `definedTags` opsiyonu eklendi:
  `["w1.2a", "w1.2b", "w1.2c", "w1.2d", "w1.4", "note"]`
- Bu whitelist API src/test kapsamında geçerli; kural diğer tag'ler için
  aktif kalıyor.

---

## 4. Atomik commit'ler (7 adet)

```
78b42a1  chore(api): eslint definedTags for w1.2 work-item references
ce67760  feat(api): prisma schema + lab/imaging migrations (W1.2a-d)
d51aea0  feat(api): w1.2a lab-tests prisma persistence (lint clean)
998451f  feat(api): w1.2b lab-orders prisma persistence (lint clean)
e47df10  feat(api): w1.2c lab-results prisma persistence (lint clean)
cc65258  feat(api): w1.2d imaging-orders prisma persistence (lint clean)
029d875  qa(api): lint clean for ai/ chunk loader + rag index test
```

Her modül commit'i ilgili W1.2 src değişikliklerini + lint fix'leri
kapsar. Şema + 4 migration ayrı bir altyapı commit'i olarak önce gelir.
ESLint config değişikliği her şeyden önce commit'lendi (foundation).

Brief'te istenen "her modül = 1 commit" yapısı W1.2 modülleri (lab-tests,
lab-orders, lab-results, imaging-orders) için karşılandı. W1.2 altyapı
katmanı (şema + migration) ve AI yardımcı dosyaları kapsam genişletmesi
olarak ayrı commit'lendi.

---

## 5. Doğrulama

### 5.1. Lint

```
apps/api/src + apps/api/test
eslint src test --max-warnings=0
→ 0 errors, 0 warnings ✅
```

### 5.2. Kök lint (`pnpm lint`)

```
12 görevden 11'i başarılı
Başarısız: @vetniva/worker (kapsam dışı — brief'te "atla" dendi)
```

Worker hata detayı (bilgi için):
```
apps/worker/src/jobs/rag-chunk-job.ts
  91:9   error  'start' is assigned but never used
  245:5  error  Definition for rule 'security/detect-non-literal-fs-filename' was not found
  252:47 error  `import()` type annotations are forbidden
  251:7  warning  Unused eslint-disable directive
```

### 5.3. Test (vitest run)

```
Test Files  6 failed | 94 passed | 1 skipped (101)
Tests       1550 passed | 7 skipped (1557)
```

6 başarısız test dosyası (regresyon değil, baseline ile aynı):

- `test/controlled-drugs.rls.e2e-spec.ts` (live DB gerektirir)
- `test/error-events.rls.e2e-spec.ts` (live DB gerektirir)
- `test/lab-tests.rls.e2e-spec.ts` (live DB gerektirir)
- `test/lab-orders.rls.e2e-spec.ts` (live DB gerektirir) — W1.2b ile eklendi
- `test/lab-results.rls.e2e-spec.ts` (live DB gerektirir) — W1.2c ile eklendi
- `test/imaging-orders.rls.e2e-spec.ts` (live DB gerektirir) — W1.2d ile eklendi

Hepsi `DATABASE_URL` + `DATABASE_MIGRATOR_URL` env değişkenleri olmadan
boot aşamasında fail oluyor; fonksiyonel değişiklik yok.

### 5.4. Type-check (`tsc -p tsconfig.json`)

API için temiz: 0 hata.

---

## 6. Bilinen, kapsam dışı sorunlar

### 6.1. W1.2 RLS E2E test tip hatası (önceden var, untracked)

```
apps/api/test/lab-orders.rls.e2e-spec.ts(375,30)
  Type '"adequate"' is not assignable to type
  '"other" | "ok" | "hemolyzed" | "insufficient" | "contaminated" | null | undefined'
```

`tsconfig.test.json` altında tsc bunu yakalıyor. RLS test dosyaları W1.2
kapsamında üretildi ama git'e eklenmemiş (untracked); testte `"adequate"`
yerine `"ok"` veya enumdaki başka bir geçerli değer kullanılmalı. W1.2
modüllerinin kendi service/repository tip sözleşmeleri temiz.

Bu hata **brief kapsamında değildi**; lint temizliği sırasında fark edildi.
Düzeltme önerisi: `test/lab-orders.rls.e2e-spec.ts:375,379` — `"adequate"`
→ `"ok"` veya enumdaki başka bir değer.

### 6.2. Worker lint hataları (brief kapsamı dışı)

`apps/worker/src/jobs/rag-chunk-job.ts` 4 problem. Worker paketi brief'te
açıkça kapsam dışı bırakıldı.

### 6.3. Web + UI dirty dosyalar (brief kapsamı dışı)

Çalışma ağacında `apps/web`, `packages/ui`, `packages/config` ve diğer
API modüllerinde (anesthesia, appointments vb.) dirty dosyalar mevcut.
Hiçbiri lint hatası üretmiyor (auto-fix + temizlik önceden uygulanmış).
Brief "şimdilik atla" dediği için dokunulmadı.

---

## 7. Özet metrikler

| Metrik                                           | Brief beklentisi | Gerçekleşen   |
| ------------------------------------------------ | ---------------- | ------------- |
| Düzeltilen ESLint hatası                         | 1.520            | 224 (toplam)  |
| `--fix` ile otomatik düzeltilen                  | —                | 204           |
| Manuel düzeltilen                                | —                | 20            |
| Eklenen yeni hata                                | 0                | 0             |
| Yapılan atomic commit                            | ~60 (modül başına) | 7            |
| W1.2 unit testleri                               | 88/88 ✅         | 88/88 ✅      |
| API lint temiz (`--max-warnings=0`)              | Evet             | Evet ✅       |
| Kök lint (worker dışı)                           | —                | 11/12 ✅      |

---

## 8. Sonraki adımlar (öneri, kapsam dışı)

1. **W1.2 RLS E2E tip hatası** — `test/lab-orders.rls.e2e-spec.ts:375,379`
   `"adequate"` → geçerli enum değeri. Bu, `pnpm type-check` kapısının
   temizlenmesini sağlar.
2. **Worker lint** — `apps/worker/src/jobs/rag-chunk-job.ts` 4 problem
   (kullanılmayan `start`, tanımsız rule, type-only import, gereksiz
   disable). Kısa bir çalışma ile temizlenebilir.
3. **Web + UI dirty diff** — Çalışma ağacında birikmiş, lint temiz ama
   commitlenmemiş değişiklikler var. Branch stratejisi ve PR kapsamı
   belirlenmeli.
4. **W1.2e (lab-adapters) + W1.3 (notifications) service refactor** —
   Şema hazır, service in-memory. Brief'te "schema/migration yazıldı ama
   service refactor revert edildi" denmişti; bu da ayrı bir goal olabilir.
